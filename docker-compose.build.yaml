# Example
services:
  # build-dms-jackpot:
    # image: fedora:40
    # volumes:
      # - ./local_sources/dms-jackpot:/app
      # - ./built_services/dms-jackpot:/app/output/artefact
    # working_dir: /app
    # command: >
      # /bin/sh -c "
      # dnf -y update &&
      # dnf -y install qt6-qtbase qt6-qtbase-mysql qt6-qtbase-devel gcc g++ make &&
      # dnf clean all &&
      # mkdir -p /app/output && 
      # cd output &&
      # /usr/lib64/qt6/bin/qmake ../dms-jackpot.pro && 
      # make -j$(nproc) &&
      # cp DMS-Jackpot /app/output/artefact &&
      # chmod -R 777 /app/output"
